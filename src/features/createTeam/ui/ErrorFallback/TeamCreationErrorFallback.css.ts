import { style } from "@vanilla-extract/css"

export const teamCreationErrorFallbackContainer = style({
  textAlign: "center",
})

export const teamCreationErrorFallbackLottieWrapper = style({
  width: "80%",
  margin: "0 auto",
})
